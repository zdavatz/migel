= migel

* http://scm.ywesee.com/?p=migel/.git;a=summary

== DESCRIPTION:

migel gem for ch.oddb.org

== FEATURES/PROBLEMS:

== REQUIREMENTS:

Use bundler to install all dependencies mentioned in the Gemfile

* bundle install

== INSTALL:

* sudo gem install migel

== TEST/COVERAGE

* gem install bundler && bundle install && rspec spec/suite.rb

== DEVELOPERS:

* Masaomi Hatakeyama
* Zeno Davatz

== LICENSE:

* GPLv2
