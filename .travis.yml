language: ruby
before_install:
  - gem install bundler

install:
  - bundle install --without debugger

script:
  - bundle exec rspec spec/suite.rb

notifications:
  email:
  - ngiger@ywesee.com
rvm:
  - ruby-head
  - 2.1.7
  - 2.2.3
  - 1.9.3
matrix:
  allow_failures:
    - rvm: ruby-head
